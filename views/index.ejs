<!-- // /Projects/views/index.ejs -->

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Snipify</title>
    <!-- Import Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous" />
    <!-- //Import Bootstrap -->
    <!-- Import Custom CSS -->
    <link rel="stylesheet" href="css/style.css" type="text/css" />
    <!-- //Import Custom CSS -->

    <!-- import Fontawesome icon -->
    <script src="https://kit.fontawesome.com/777c5149ce.js" crossorigin="anonymous"></script>
    <!-- //import Fontawesome icon -->

    <style></style>
  </head>
  <body class="bg-dark text-white">
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary py-2">
      <div class="container">
        <a class="navbar-brand fs-3 fw-bold" href="#"><i class="fa-solid fa-scissors me-1"></i>Snipify</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0"></ul>
          <form class="d-flex">
            <a href="/admin" class="btn btn-outline-light me-2">Admin Section</a>
          </form>
        </div>
      </div>
    </nav>

    <div class="container border border-secondary-3 rounded my-5 p-5">
      <h1 class="text-center mb-4 mt-5 fw-bolder pt-5 pb-3">Shorten, Share, Simplify !</h1>

      <form action="/shortUrl" method="POST" class="row g-3 justify-content-center">
        <div class="col-md-6 mx-auto">
          <label for="fullUrl" class="form-label fw-semibold">Input URL</label>
          <div class="input-group mb-5 pb-5 pt-2">
            <input required placeholder="https://www.example.com" type="url" name="fullUrl" id="fullUrl" class="form-control" />
            <button type="submit" class="btn btn-primary px-4">Shrink</button>
          </div>
        </div>
      </form>

      <% if(showShortenedLink && shortUrls.length > 0) { %>
      <div>
        <h4 class="my-5 text-center">Your Shortened Link is <a href="<%= shortUrls[0].short %>"><%= shortUrls[0].short %></a></h4>
      </div>
      <% } %>
    </div>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  </body>
</html>
